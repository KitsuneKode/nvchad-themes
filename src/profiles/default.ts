import type { ZedProfileOverride } from "./types.ts";

export const defaultDarkProfile: ZedProfileOverride = { id: "default-dark" };
export const defaultLightProfile: ZedProfileOverride = { id: "default-light" };
