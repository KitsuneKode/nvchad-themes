import type { ZedProfileOverride } from "./types.ts";

/** Values from ethangilmore/zed-kanagawa (Kanagawa Wave). */
export const kanagawaProfile: ZedProfileOverride = {
  id: "kanagawa",
  git: {
    textMuted: "#727169",
    statusIgnored: "#4F4F66",
    icon: "#DCD7BA"
  }
};
